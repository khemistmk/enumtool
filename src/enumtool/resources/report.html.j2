<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>EnumTool Report - {{ result.domain }}</title>
  <style>
    :root { --bg:#0f172a; --fg:#e2e8f0; --muted:#94a3b8; --accent:#38bdf8; --ok:#10b981; --warn:#f59e0b; --bad:#ef4444; }
    body { margin:0; font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, sans-serif; background:var(--bg); color:var(--fg); }
    header { padding: 20px 24px; border-bottom:1px solid #1f2937; }
    h1 { margin:0; font-size: 20px; }
    .meta { color: var(--muted); font-size: 13px; }
    main { padding: 24px; }
    section { margin-bottom: 28px; }
    table { width: 100%; border-collapse: collapse; }
    th, td { border-bottom: 1px solid #1f2937; padding: 8px 10px; vertical-align: top; }
    th { text-align: left; color: var(--muted); font-weight: 600; font-size: 12px; letter-spacing: .02em; }
    tr:hover td { background: #0b1220; }
    .tag { display:inline-block; padding:2px 6px; border-radius: 6px; background:#1f2937; color:#cbd5e1; margin-right:6px; margin-bottom:4px; font-size:12px; }
    .mono { font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
    .small { font-size: 12px; color: var(--muted); }
  </style>
  <script>function toggle(id){const el=document.getElementById(id); el.hidden=!el.hidden;}</script>
  </head>
<body>
  <header>
    <h1>EnumTool Report · {{ result.domain }}</h1>
    <div class="meta">Generated at {{ generated_at }}</div>
  </header>
  <main>
    <section>
      <h2>Apex</h2>
      <table>
        <tr><th>WHOIS</th><td>
          {% if result.whois.registrar %}<span class="tag">Registrar: {{ result.whois.registrar }}</span>{% endif %}
          {% if result.whois.creation_date %}<span class="tag">Created: {{ result.whois.creation_date }}</span>{% endif %}
          {% if result.whois.expiration_date %}<span class="tag">Expires: {{ result.whois.expiration_date }}</span>{% endif %}
        </td></tr>
        <tr><th>DNS</th><td class="mono">
          A: {{ result.records.a|join(', ') }}<br/>
          AAAA: {{ result.records.aaaa|join(', ') }}<br/>
          CNAME: {{ result.records.cname|join(', ') }}<br/>
          NS: {{ result.records.ns|join(', ') }}<br/>
          MX: {{ result.records.mx|join(', ') }}<br/>
          TXT: {{ result.records.txt|join(' | ') }}
        </td></tr>
      </table>
    </section>

    <section>
      <h2>Subdomains</h2>
      <table>
        <thead>
          <tr>
            <th>Subdomain</th>
            <th>IPs</th>
            <th>Ports</th>
            <th>Web</th>
          </tr>
        </thead>
        <tbody>
        {% for s in result.subdomains %}
          <tr>
            <td class="mono">{{ s.name }}</td>
            <td class="mono">{{ s.ips|join(', ') }}</td>
            <td class="mono">
              {% set open_ports = s.ports | selectattr('open') | list %}
              {% if open_ports %}
                {% for p in open_ports %}
                  <span class="tag">{{ p.port }} open</span>
                {% endfor %}
              {% else %}
                <span class="small">none</span>
              {% endif %}
            </td>
            <td>
              {% for key, h in s.http.items() %}
                <div>
                  <a class="mono" href="{{ h.url }}" target="_blank">{{ h.url }}</a>
                  {% if h.status %}<span class="small">status {{ h.status }}</span>{% endif %}
                  {% if h.server %}<span class="tag">{{ h.server }}</span>{% endif %}
                  {% if h.title %}<span class="small">— {{ h.title }}</span>{% endif %}
                  {% if h.tech %}<div class="small">{{ h.tech|join(', ') if h.tech is iterable else h.tech }}</div>{% endif %}
                </div>
              {% endfor %}
            </td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
    </section>
  </main>
</body>
</html>
